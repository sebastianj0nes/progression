export type Entry = {
    date: string
    feelings: string,
    goals: string,
    accomplishments: string,
    score: string
}

export type User = {
    id: string;
    name: string;
    email: string;
    password: string;
};